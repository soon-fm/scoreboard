# Docker Compose Data Directory

When running using `docker-compose`, this directory is used to store
peristent data from `redis` and `influxdb`.

All files, other than this file will be ignored in commits.
